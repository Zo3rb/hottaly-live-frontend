import React, { Fragment } from 'react';

// Internal Imports
import LoginForm from '../components/LoginForm';

const Login = () => {
    return (
        <Fragment>
            <LoginForm />
        </Fragment>
    );
}

export default Login;
